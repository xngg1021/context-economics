# Deployment-Aware Cost — Brainstorm Addendum (2026-09-09)

状态：脑暴记录，未进入实现。记录一次研究范围扩展的讨论，供后续版本规划引用。

## 动机

CE 的 L0-L6 以云端 API 调用为前提建立，成本是标量：单价乘以 token 数，云厂商把电费、机房、折旧全部折进单价里藏起来了。当部署形态是本地设备时，这些隐藏项全部显性化，成本结构完全不同。本地部署下成本不是"多少钱"，是一组并行消耗的资源。

## 核心命题

成本从标量升级成向量。部署形态就是向量的计价函数。云端、本地、混合是三种计价函数，不是三种框架。

同一个成功任务（cost_per_success 的单位不变），在不同部署形态下消耗不同的资源组合。CE 的"成功任务"计价单位不需要改动，需要改动的是成本向量的成分和计价方式。

## 客观维度清单

以下维度可以直接测量，进入成本向量：

- 电费。一度电的价格明确，本地推理的瓦时可以直接测量。
- 硬件折旧。硬件价格和预期寿命可算，可以摊到每次推理。
- 机会成本。设备被 AI 占用时无法执行其他工作，GPU 是典型场景。
- FLOPs 与延迟。本地小模型的慢响应是真实时间成本。
- 空间与散热。物理占位和散热负担可以折算为基础设施成本。

## 主观维度的处理原则

主观维度（噪音、占地感受、隐私安心感）不做数值化处理。这是设计决策，不是技术限制。

理由：给主观感受标 0-100 的量级，等于把人的感受变成可排序、可替换的商品，剥夺感受的不可通约性。主观部分就是判断，判断权留在人手里。

处理方式采用显示性偏好：系统只计算客观账，把选项摆在部署者面前，由人凭感受拍板。系统记录每次的选择行为本身，不对选择背后的感受打分。账本写的是"这个人在这些情形下选择了什么"，不是"这个人的心情量化值"。

行为是证据，自我报告不是。这个原则与 Family HF 的"系统持有上下文，人类持有判断"一致，也与 THM 的行为证据哲学一致。

## 聚焦入口

Deployment break-even 问题：给定一个任务流，把全部客观维度折进去之后，什么条件下本地部署的总成本低于云端部署。这个问题有确定答案、可复现、可做实验，适合作为扩展的第一个落地题目。

## 定位差

OpenJarvis 的 Intelligence Per Watt 做单点硬件效率排行（每瓦特、每 FLOPs 的正确率）。云厂商算 API 账单。任务级的部署总账（同一个任务流在不同部署形态下的全维度成本对比）目前没有人算。这个空位和 CE 的 cost_per_success 单位严丝合缝。

## 落地路径

- L5 external cost 这个筐是现成的扩展位，部署感知的成本项可以先进这里。
- L0 定价层加部署感知维度：同一个成功任务在云端、本机、混合模式下分别消耗什么。
- 测量平台：本地工作站（HP Z6 G4，Xeon 双路加 RTX 3080）实测瓦时每 token、任务时长、占用率。
- 复用 THM 的基准基础设施和硬件一致性校验方法。

## 非目标

不做 ESG 式的社会外部性账，只算对部署者本人的账。不做主观效用的数值化。不开新仓库，本扩展作为 CE 框架内的一个维度落地。

## 与现有层的映射

L0 Pricing：部署感知定价模型，云端定价与本地资源折价的对照。
L1 Serving：本地推理的 prefill 与 KV 复用成本与云端缓存经济学的对照。
L5 Task Economics：external cost 扩展为部署感知的成本向量。
L6 Adaptive Control：deployment break-even 作为影子建议的一种新形式。
